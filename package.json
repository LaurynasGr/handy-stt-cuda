{
  "name": "handy-stt-cuda",
  "type": "module",
  "private": true,
  "scripts": {
    "lint": "concurrently 'bun biome check' 'tsc --noEmit'",
    "lint:fix": "bun biome check --fix && bun run lint"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.1",
    "@types/bun": "latest",
    "concurrently": "^10.0.3",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "picocolors": "^1.1.1"
  }
}
